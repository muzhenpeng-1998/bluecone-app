<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluecone.app.campaign.infrastructure.persistence.mapper.CampaignMapper">
    
    <select id="selectAvailableCampaigns" resultType="com.bluecone.app.campaign.infrastructure.persistence.po.CampaignPO">
        SELECT *
        FROM bc_campaign
        WHERE tenant_id = #{tenantId}
          AND campaign_type = #{campaignType}
          AND status = #{status}
          AND start_time &lt;= #{queryTime}
          AND (end_time IS NULL OR end_time &gt; #{queryTime})
        ORDER BY priority DESC, id ASC
    </select>
    
</mapper>
